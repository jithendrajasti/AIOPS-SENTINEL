const { Client } = require('pg'); async function main() { const client = new Client({ connectionString: 'postgresql://aiops:aiops_secret@localhost:5432/aiops_sentinel' }); await client.connect(); const res = await client.query('SELECT column_name, data_type FROM information_schema.columns WHERE table_name = "GoldenRecord"'); console.log(res.rows); await client.end(); } main().catch(console.error);
