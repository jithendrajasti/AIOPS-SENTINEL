# Kafka broker address (host machine access)
KAFKA_BROKERS=localhost:9092

# Milliseconds between normal log lines (default 300ms)
LOG_INTERVAL_MS=300

# Every N ms, inject a burst of errors from one service to trigger anomaly detection
BURST_INTERVAL_MS=30000

# How many error lines to inject per burst
BURST_SIZE=25
